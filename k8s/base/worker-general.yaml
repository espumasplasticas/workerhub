apiVersion: apps/v1
kind: Deployment
metadata:
  name: workerhub-worker-general
  namespace: workerhub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: workerhub-worker-general
  template:
    metadata:
      labels:
        app: workerhub-worker-general
    spec:
      containers:
        - name: queue-worker
          image: ghcr.io/comodisimos/workerhub-php-k8s:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: workerhub-config
            - secretRef:
                name: workerhub-secrets
          command:
            - sh
            - -lc
            - php artisan queue:work redis --queue=general-high,general-default,migration-high,migration-default --sleep=1 --tries=3 --timeout=300 --backoff=5 --max-time=3600
---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: workerhub-worker-general
  namespace: workerhub
spec:
  scaleTargetRef:
    name: workerhub-worker-general
  minReplicaCount: 1
  maxReplicaCount: 6
  cooldownPeriod: 60
  pollingInterval: 15
  triggers:
    - type: redis
      metadata:
        addressFromEnv: KEDA_REDIS_ADDRESS
        listName: queues:general-high
        listLength: "1"
        activationListLength: "1"
        passwordFromEnv: REDIS_PASSWORD
    - type: redis
      metadata:
        addressFromEnv: KEDA_REDIS_ADDRESS
        listName: queues:general-default
        listLength: "5"
        activationListLength: "1"
        passwordFromEnv: REDIS_PASSWORD
    - type: redis
      metadata:
        addressFromEnv: KEDA_REDIS_ADDRESS
        listName: queues:migration-default
        listLength: "5"
        activationListLength: "1"
        passwordFromEnv: REDIS_PASSWORD
